import { FC } from "react";
import "./NavBar.css";

const NavBar: FC = () => {
  return (
    <div className="nav-bar">
      <div className="nav-bar-title">
        WEBPHONE
      </div>
    </div>
  );
};

export default NavBar;
